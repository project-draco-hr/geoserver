{
  String id=function.getIdentifier().toString();
  if (functionMap.containsKey(id))   throw new IllegalArgumentException("function already exists");
  if (superset != null)   superset.addFunction(function);
  functionMap.put(id,function);
}
