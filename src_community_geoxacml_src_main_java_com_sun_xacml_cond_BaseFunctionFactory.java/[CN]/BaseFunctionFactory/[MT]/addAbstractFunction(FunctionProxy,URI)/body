{
  String id=identity.toString();
  if (functionMap.containsKey(id))   throw new IllegalArgumentException("function already exists");
  if (superset != null)   superset.addAbstractFunction(proxy,identity);
  functionMap.put(id,proxy);
}
