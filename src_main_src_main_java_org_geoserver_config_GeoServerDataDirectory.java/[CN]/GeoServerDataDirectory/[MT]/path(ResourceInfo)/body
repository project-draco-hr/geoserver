{
  StoreInfo store=resource.getStore();
  WorkspaceInfo workspace=store.getWorkspace();
  String path=Paths.path("workspaces",workspace.getName(),store.getName(),resource.getName());
  return path;
}
