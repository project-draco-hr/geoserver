{
  if (create) {
    Resource resource=get(pathNamespaceFile(ws));
    return resource.file();
  }
 else {
    Resource resource=get(pathNamespaceFile(ws));
    return Resources.file(resource);
  }
}
