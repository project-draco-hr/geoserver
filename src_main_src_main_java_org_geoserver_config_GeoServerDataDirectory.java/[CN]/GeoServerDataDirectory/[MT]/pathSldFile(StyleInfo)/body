{
  WorkspaceInfo workspace=style != null ? style.getWorkspace() : null;
  if (workspace == null) {
    return Paths.convert("styles",style.getFilename());
  }
 else {
    return Paths.convert(Paths.path("workspaces",workspace.getName(),"styles"),style.getFilename());
  }
}
