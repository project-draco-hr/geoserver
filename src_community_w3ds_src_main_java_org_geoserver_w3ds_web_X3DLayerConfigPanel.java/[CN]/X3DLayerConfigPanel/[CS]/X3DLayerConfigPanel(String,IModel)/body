{
  super(id,model);
  initActiveLayer(model);
  initQueryable(model);
  initObjectID(model);
  initObjectClass(model);
  initTiled(model);
  initTileSets(model);
  initHLOD(model);
  initLODSet(model);
  initStyles(model);
  initDefaultStyle(model);
}
