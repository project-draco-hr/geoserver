{
  LockFeature lockFeature=new LockFeature(getServiceInfo(),getCatalog(),filterFactory);
  return (LockFeatureResponseType)lockFeature.lockFeature(new LockFeatureRequest.WFS20(request)).getAdaptee();
}
