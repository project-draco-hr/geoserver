{
  testPostSLDPackage();
  Catalog cat=getCatalog();
  assertNotNull(cat.getStyleByName("foo"));
  MockHttpServletResponse response=getAsServletResponse("/rest/styles/foo.zip");
  assertEquals(200,response.getStatusCode());
}
