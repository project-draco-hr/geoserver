{
  Object[] list=inputs.toArray();
  for (int i=0; i < list.length; i++)   if (((Evaluatable)(list[i])).returnsBag())   throw new IllegalArgumentException("n-of can't use bags");
  checkInputsNoBag(inputs);
}
