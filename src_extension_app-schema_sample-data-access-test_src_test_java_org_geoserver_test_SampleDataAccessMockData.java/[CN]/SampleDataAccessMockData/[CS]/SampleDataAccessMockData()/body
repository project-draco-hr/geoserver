{
  data=IOUtils.createRandomDirectory("./target","sample-data-access-mock","data");
  data.delete();
  data.mkdir();
  featureTypes=new File(data,"featureTypes");
  featureTypes.mkdir();
  info(DATASTORE_NAME,SampleDataAccessData.NAMESPACE_PREFIX,SampleDataAccessData.MAPPEDFEATURE_TYPE_NAME.getLocalPart());
  info(DATASTORE_NAME,SampleDataAccessData.NAMESPACE_PREFIX,SampleDataAccessData.GEOLOGICUNIT_TYPE_NAME.getLocalPart());
}
