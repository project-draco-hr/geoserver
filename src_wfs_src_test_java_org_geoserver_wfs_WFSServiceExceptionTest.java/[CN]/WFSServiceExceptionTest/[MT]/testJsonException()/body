{
  String path="wfs/?service=wfs" + "&version=1.1.0" + "&request=DescribeFeatureType"+ "&typeName=foobar"+ "&EXCEPTIONS=" + JSONType.jsonp + "&format_options=callback:myMethod";
  MockHttpServletResponse response=getAsServletResponse(path);
  String content=response.getOutputStreamContent();
  testJson(testJsonP(content));
}
