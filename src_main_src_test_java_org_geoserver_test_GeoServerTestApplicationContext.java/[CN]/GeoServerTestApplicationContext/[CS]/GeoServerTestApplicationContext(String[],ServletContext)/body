{
  super(configLocation,false);
  setValidating(false);
  try {
    servletContext.setAttribute("javax.servlet.context.tempdir",IOUtils.createRandomDirectory("./target","mock","tmp"));
  }
 catch (  Exception e) {
    throw new RuntimeException(e);
  }
  this.servletContext=servletContext;
}
