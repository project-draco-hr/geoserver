{
  final TYPE type=getType(layer);
switch (type) {
case RASTER:
    return RASTER_ICON;
case VECTOR:
  return VECTOR_ICON;
case LAYERGROUP:
return GROUP_ICON;
case WMS:
return MAP_ICON;
default :
return UNKNOWN_ICON;
}
}
