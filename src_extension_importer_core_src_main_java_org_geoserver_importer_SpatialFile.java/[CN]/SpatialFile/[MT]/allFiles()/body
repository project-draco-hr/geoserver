{
  ArrayList<Resource> all=new ArrayList<Resource>();
  all.add(file);
  if (prjFile != null) {
    all.add(prjFile);
  }
  if (styleFile != null) {
    all.add(styleFile);
  }
  all.addAll(suppFiles);
  return all;
}
