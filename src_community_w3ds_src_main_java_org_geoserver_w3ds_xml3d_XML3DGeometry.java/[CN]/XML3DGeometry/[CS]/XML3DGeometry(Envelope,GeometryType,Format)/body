{
  verticesHashMap=new HashMap<String,Integer>(64000);
  vertices=new ArrayList<Vector3>();
  requestFormat=format;
  geometryType=type;
  bbox=new Double[4];
  bbox[0]=bBox.getLowerCorner().getCoordinate()[0];
  bbox[1]=bBox.getLowerCorner().getCoordinate()[1];
  bbox[2]=bBox.getUpperCorner().getCoordinate()[0];
  bbox[3]=bBox.getUpperCorner().getCoordinate()[1];
}
