{
  try {
    return FileUtils.readFileToString(file);
  }
 catch (  IOException ex) {
    LOGGER.warning(String.format("Error reading file '%s' because ",file.getAbsolutePath(),ex.getMessage()));
  }
  return "";
}
