{
  try {
    return new ArrayList<String>(GeoServerApplication.get().getSecurityManager().listFilters(GeoServerAuthenticationFilter.class));
  }
 catch (  IOException e) {
    throw new WicketRuntimeException(e);
  }
}
