{
  if (writer != null && stream != null) {
    getOutputStream();
  }
  getResponse().flushBuffer();
  if (writer != null) {
    writer.flush();
  }
 else   if (stream != null) {
    stream.flush();
  }
}
