{
  if (m_attributesToInclude.contains(attr.getName().toString())) {
    LOGGER.log(Level.FINER,"sha1 += {0}({1})",new Object[]{attr.getName(),value.toString()});
    m_sha1.update(value.toString().getBytes(UTF8));
  }
}
