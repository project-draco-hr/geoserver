{
  if (inline) {
    int mapLayerOrder=mapContext.indexOf(mapLayer);
    element("href","images/layer_" + mapLayerOrder + ".png");
  }
 else {
    element("href",WMSRequests.getGetMapUrl(mapContext.getRequest(),mapLayer,0,mapContext.getAreaOfInterest(),new String[]{"format","image/png","transparent","true"}));
  }
}
