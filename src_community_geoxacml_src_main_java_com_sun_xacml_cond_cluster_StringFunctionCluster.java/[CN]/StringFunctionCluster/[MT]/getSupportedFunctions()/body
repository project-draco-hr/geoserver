{
  Set<Function> set=new HashSet<Function>();
  for (  String fn : StringFunction.getSupportedIdentifiers())   set.add(new StringFunction(fn));
  set.add(new URLStringCatFunction());
  return set;
}
