{
  boolean modified=false;
  FeatureIterator<? extends F> e=c.features();
  try {
    while (e.hasNext()) {
      if (add(e.next()))       modified=true;
    }
  }
  finally {
    e.close();
  }
  return modified;
}
