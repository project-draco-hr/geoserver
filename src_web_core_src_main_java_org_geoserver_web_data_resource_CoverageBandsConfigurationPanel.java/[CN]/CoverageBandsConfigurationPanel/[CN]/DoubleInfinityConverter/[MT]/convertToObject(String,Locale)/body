{
  final Number number=parse(value,BigDecimal.valueOf(Double.NEGATIVE_INFINITY),BigDecimal.valueOf(Double.POSITIVE_INFINITY),Locale.US);
  if (number == null) {
    return null;
  }
  return new Double(number.doubleValue());
}
