{
  secure="HTTPS".equalsIgnoreCase(casUrlPrefix.getProtocol());
  this.casUrlPrefix=casUrlPrefix;
  if (proxyReceptor != null && "HTTPS".equalsIgnoreCase(proxyReceptor.getProtocol()) == false)   throw new RuntimeException("proxy receptor url must be HTTPS");
  this.proxyReceptor=proxyReceptor;
}
