{
  if (m_image != null) {
    Raster tile=getTile(x,y);
    if (tile != null) {
      return tile.getSampleDouble(x,y,band);
    }
 else {
      return getNoDataValue();
    }
  }
 else {
    return getNoDataValue();
  }
}
