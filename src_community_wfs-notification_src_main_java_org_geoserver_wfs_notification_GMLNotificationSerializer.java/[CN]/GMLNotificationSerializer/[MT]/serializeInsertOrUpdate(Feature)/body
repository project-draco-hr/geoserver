{
  try {
    String rawMessage;
    rawMessage=getInsertUpdateRawMessage(f);
    debugMessage(rawMessage);
    return rawMessage;
  }
 catch (  Throwable t) {
    onFailure(t);
  }
 finally {
    updates.incrementAndGet();
  }
  return null;
}
