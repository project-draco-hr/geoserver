{
  for (  Resource r : artifactsStore.listExecutionResourcess()) {
    LOGGER.log(Level.SEVERE,"We still need to check if the execution id is associated " + "to something still running for: " + r.name());
    cleanupResource(r,expirationThreshold);
  }
}
