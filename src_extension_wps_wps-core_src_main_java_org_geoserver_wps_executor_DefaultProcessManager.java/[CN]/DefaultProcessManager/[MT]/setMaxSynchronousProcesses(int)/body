{
  if (synchService == null) {
    synchService=new ThreadPoolExecutor(1,maxSynchronousProcesses,0L,TimeUnit.MILLISECONDS,new LinkedBlockingQueue<Runnable>());
  }
 else {
    synchService.setMaximumPoolSize(maxSynchronousProcesses);
  }
}
