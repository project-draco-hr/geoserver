{
  if (asynchService == null) {
    asynchService=new ThreadPoolExecutor(1,maxAsynchronousProcesses,0L,TimeUnit.MILLISECONDS,new LinkedBlockingQueue<Runnable>());
  }
 else {
    asynchService.setMaximumPoolSize(maxAsynchronousProcesses);
  }
}
