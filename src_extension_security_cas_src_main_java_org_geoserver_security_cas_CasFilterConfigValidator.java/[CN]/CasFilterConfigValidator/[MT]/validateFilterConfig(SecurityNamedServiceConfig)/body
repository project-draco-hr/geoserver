{
  if (config instanceof CasAuthenticationFilterConfig)   validateFilterConfig((CasAuthenticationFilterConfig)config);
  if (config instanceof CasProxiedAuthenticationFilterConfig)   validateFilterConfig((CasProxiedAuthenticationFilterConfig)config);
  super.validateFilterConfig(config);
}
