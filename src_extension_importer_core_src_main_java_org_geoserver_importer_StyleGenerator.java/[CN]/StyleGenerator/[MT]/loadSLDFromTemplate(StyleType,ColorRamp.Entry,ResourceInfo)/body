{
  String colorCode=Integer.toHexString(entry.color.getRGB());
  colorCode=colorCode.substring(2,colorCode.length());
  return TEMPLATES.get(type).replace("${colorName}",entry.name).replace("${colorCode}","#" + colorCode).replace("${layerName}",resource.getName());
}
