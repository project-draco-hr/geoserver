{
  JDBCConfigProperties config=loadConfig();
  if (!config.isEnabled()) {
    LOGGER.info("jdbcconfig is disabled");
    return config;
  }
  if (config.isInitDb()) {
    copyScriptsToDataDir();
  }
  return config;
}
