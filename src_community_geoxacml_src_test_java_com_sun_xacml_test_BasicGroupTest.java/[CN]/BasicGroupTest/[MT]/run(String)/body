{
  Iterator<Test> it=tests.iterator();
  int errorCount=0;
  System.out.println("Running group " + name);
  while (it.hasNext())   errorCount+=it.next().run(testPrefix + name);
  System.out.println("Finished group " + name + " [failures: "+ errorCount+ "]");
  return errorCount;
}
