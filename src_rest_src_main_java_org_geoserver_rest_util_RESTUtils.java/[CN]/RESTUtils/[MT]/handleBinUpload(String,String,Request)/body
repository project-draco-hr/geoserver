{
  GeoServerResourceLoader loader=GeoServerExtensions.bean(GeoServerResourceLoader.class);
  Resource data=loader.get("data");
  final File dir=data.dir();
  return handleBinUpload(datasetName + "." + extension,null,dir,request);
}
