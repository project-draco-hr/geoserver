{
  try {
    if (branches.isEmpty())     return null;
 else     if (branches.size() == 1)     return branches.get(0);
 else     return FF.or(branches);
  }
  finally {
    branches=new ArrayList<Filter>();
  }
}
