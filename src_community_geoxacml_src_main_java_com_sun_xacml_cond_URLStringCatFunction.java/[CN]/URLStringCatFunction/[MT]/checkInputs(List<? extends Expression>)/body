{
  Iterator<? extends Expression> it=inputs.iterator();
  while (it.hasNext()) {
    if (it.next().returnsBag())     throw new IllegalArgumentException(NAME_URI_STRING_CONCATENATE + " doesn't accept bags");
  }
  checkInputsNoBag(inputs);
}
