{
  Format format=request.getValidFormat(formatstr);
  if (format == null) {
    throw new IllegalArgumentException("Invalid parameter FORMAT (invalid format mime-type): " + formatstr);
  }
  return format;
}
