{
  byte[] left=get();
  byte[] right=o.get();
  int i=0;
  while (i < left.length) {
    if (i == right.length) {
      return 1;
    }
    int diff=(0xFF & left[i]) - (0xFF & right[i]);
    if (diff != 0) {
      return diff;
    }
    i++;
  }
  if (i < right.length) {
    return -1;
  }
  return 0;
}
