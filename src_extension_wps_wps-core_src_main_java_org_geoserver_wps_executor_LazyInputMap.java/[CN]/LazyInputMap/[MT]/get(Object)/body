{
  InputProvider provider=providers.get((String)key);
  if (provider == null) {
    return null;
  }
 else {
    try {
      return provider.getValue();
    }
 catch (    Exception e) {
      throw new WPSException("Failed to retrieve value for input " + provider.getInputId());
    }
  }
}
