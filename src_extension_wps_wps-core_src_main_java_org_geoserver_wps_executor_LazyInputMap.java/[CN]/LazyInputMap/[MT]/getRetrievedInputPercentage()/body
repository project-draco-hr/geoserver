{
  if (providers.size() == 0) {
    return 1.0f;
  }
  float count=0;
  for (  InputProvider provider : providers.values()) {
    if (provider.resolved()) {
      count++;
    }
  }
  return count / providers.size();
}
