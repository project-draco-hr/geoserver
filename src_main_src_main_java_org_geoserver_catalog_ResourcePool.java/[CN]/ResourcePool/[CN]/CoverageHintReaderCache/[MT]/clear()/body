{
  for (Iterator e=entrySet().iterator(); e.hasNext(); ) {
    Map.Entry<CoverageHintReaderKey,GridCoverageReader> entry=(Entry<CoverageHintReaderKey,GridCoverageReader>)e.next();
    dispose(entry.getKey().info,entry.getValue());
  }
  super.clear();
}
