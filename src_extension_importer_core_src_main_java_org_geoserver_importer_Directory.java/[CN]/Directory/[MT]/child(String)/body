{
  if (name == null) {
    try {
      return Resources.createRandom("child","tmp",file);
    }
 catch (    IOException e) {
      throw new RuntimeException(e);
    }
  }
  return file.get(name);
}
