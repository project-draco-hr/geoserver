{
  if (nextDifference != null) {
    return true;
  }
  advance();
  while (sortedFids.size() > 0) {
    nextDifference=buildNextDiff();
    if (nextDifference != null && (nextDifference.getState() != FeatureDiff.UPDATED || nextDifference.getChangedAttributes().size() > 0)) {
      return true;
    }
 else {
      nextDifference=null;
      advance();
    }
  }
  return false;
}
