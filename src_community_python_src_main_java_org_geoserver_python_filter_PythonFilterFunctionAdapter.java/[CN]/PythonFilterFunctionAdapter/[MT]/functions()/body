{
  if (fw.isModified()) {
synchronized (this) {
      if (fw.isModified()) {
        try {
          functions=fw.read();
        }
 catch (        IOException e) {
          throw new RuntimeException(e);
        }
      }
    }
  }
  return functions;
}
