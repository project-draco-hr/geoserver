{
  if (s1 == null) {
    return false;
  }
  ProcessState startPhase=s1.getPhase();
  ProcessState endPhase=s2.getPhase();
  return (endPhase.equals(startPhase) || endPhase.isValidSuccessor(startPhase)) && s2.getProgress() >= s1.getProgress();
}
