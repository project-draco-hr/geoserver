{
  FeatureIterator<?> iterator=collection.features();
  try {
    while (iterator.hasNext()) {
      Feature feature=iterator.next();
      sha1One(feature);
    }
  }
  finally {
    iterator.close();
  }
}
