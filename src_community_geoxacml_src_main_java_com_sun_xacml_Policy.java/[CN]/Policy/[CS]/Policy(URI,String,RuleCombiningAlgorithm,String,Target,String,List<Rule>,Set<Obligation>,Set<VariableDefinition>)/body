{
  super(id,version,combiningAlg,description,target,defaultVersion,obligations,null);
  List<CombinerElement> list=null;
  if (rules != null) {
    list=new ArrayList<CombinerElement>();
    for (    Rule rule : rules) {
      list.add(new RuleCombinerElement(rule));
    }
  }
  setChildren(list);
  if (definitions == null)   this.definitions=Collections.emptySet();
 else   this.definitions=Collections.unmodifiableSet(new HashSet<VariableDefinition>(definitions));
}
