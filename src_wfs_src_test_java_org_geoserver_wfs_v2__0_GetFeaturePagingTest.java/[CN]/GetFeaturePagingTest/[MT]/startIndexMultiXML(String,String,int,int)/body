{
  String xml="<GetFeature version=\"2.0.0\" startIndex='" + startIndex + "'";
  if (count > -1) {
    xml+=" count='" + count + "'";
  }
  xml+=">" + " <Query typeNames='" + fifteen + "'>"+ " </Query>"+ " <Query typeNames='"+ seven+ "'>"+ " </Query>"+ "</GetFeature>";
  return xml;
}
