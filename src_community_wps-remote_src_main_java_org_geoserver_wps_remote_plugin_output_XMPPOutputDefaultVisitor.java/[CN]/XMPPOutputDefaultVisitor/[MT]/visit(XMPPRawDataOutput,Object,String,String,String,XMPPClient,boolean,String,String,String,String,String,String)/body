{
  if (value != null && value instanceof String && !((String)value).isEmpty()) {
    return visitor.produceOutput(value,type,pID,baseURL,xmppClient,publish,name,title,description,defaultStyle,targetWorkspace,metadata);
  }
  return null;
}
