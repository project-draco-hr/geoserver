{
  this.array();
  if (axisOrder == CRS.AxisOrder.NORTH_EAST) {
    this.value(y);
    this.value(x);
  }
 else {
    this.value(x);
    this.value(y);
  }
  if (!Double.isNaN(z)) {
    this.value(z);
  }
  return this.endArray();
}
