{
  this.array();
  if (axisOrder == CRS.AxisOrder.NORTH_EAST) {
    roundedValue(y);
    roundedValue(x);
  }
 else {
    roundedValue(x);
    roundedValue(y);
  }
  if (!Double.isNaN(z)) {
    roundedValue(z);
  }
  return this.endArray();
}
