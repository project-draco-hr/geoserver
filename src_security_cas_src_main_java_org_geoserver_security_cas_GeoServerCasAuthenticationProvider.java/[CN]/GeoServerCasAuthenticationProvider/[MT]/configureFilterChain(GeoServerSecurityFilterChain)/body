{
  RequestFilterChain casChain=new RequestFilterChain(GeoServerCasConstants.CAS_PROXY_RECEPTOR_PATTERN);
  casChain.setFilterNames(pgtCallback.getName());
  filterChain.getRequestChains().add(0,casChain);
}
