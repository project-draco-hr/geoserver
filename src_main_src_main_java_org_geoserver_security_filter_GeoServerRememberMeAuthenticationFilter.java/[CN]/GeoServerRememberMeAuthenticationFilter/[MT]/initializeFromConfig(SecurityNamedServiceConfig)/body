{
  super.initializeFromConfig(config);
  RememberMeAuthenticationFilter filter=new RememberMeAuthenticationFilter(getSecurityManager(),getSecurityManager().getRememberMeService());
  filter.afterPropertiesSet();
  getNestedFilters().add(filter);
}
