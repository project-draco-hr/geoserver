{
  GeoServerUser user=(GeoServerUser)service.loadUserByUsername(username);
  if (user == null)   return null;
  if (encoder.isResponsibleForEncoding(user.getPassword()))   return new UserDetailsPasswordWrapper(user,encoder.decode(user.getPassword()));
  return user;
}
