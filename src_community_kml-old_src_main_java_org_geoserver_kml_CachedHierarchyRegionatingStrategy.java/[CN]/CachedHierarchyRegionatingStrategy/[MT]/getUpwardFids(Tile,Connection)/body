{
  if (tile == null)   return Collections.EMPTY_SET;
  Set<String> fids=new HashSet();
  fids.addAll(readFeaturesForTile(tile,conn));
  fids.addAll(getUpwardFids(tile.getParent(),conn));
  return fids;
}
