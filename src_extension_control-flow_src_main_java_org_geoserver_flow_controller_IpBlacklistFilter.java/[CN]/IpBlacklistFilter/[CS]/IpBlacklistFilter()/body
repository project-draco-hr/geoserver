{
  try {
    PropertyFileWatcher configFile=new PropertyFileWatcher(new File(GeoserverDataDirectory.getGeoserverDataDirectory(),"controlflow.properties"));
    Properties props=configFile.getProperties();
    configureIpBlackList(props);
  }
 catch (  Exception e) {
    LOGGER.log(Level.FINER,e.getMessage(),e);
  }
}
