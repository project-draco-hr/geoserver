{
  if (fw.isModified()) {
synchronized (this) {
      if (fw.isModified()) {
        try {
          processes=fw.read();
        }
 catch (        IOException e) {
          throw new RuntimeException(e);
        }
      }
    }
  }
  return processes;
}
