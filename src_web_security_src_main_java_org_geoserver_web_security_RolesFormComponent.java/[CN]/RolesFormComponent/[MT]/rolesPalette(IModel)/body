{
  return new Palette("roles",choiceModel,new Model((Serializable)roles),new SimpleChoiceRenderer(),10,false){
    protected Recorder newRecorderComponent(){
      Recorder rec=super.newRecorderComponent();
      rec.setRequired(true);
      return rec;
    }
  }
;
}
