{
  SimpleFeature conflict=SimpleFeatureBuilder.build(conflicts.getSchema(),new Object[]{layer.getSchema().getTypeName(),id.substring(id.lastIndexOf(".") + 1),newLocalRevision,new Date(),'m',null,null},null);
  conflicts.addFeatures(collection(conflict));
}
