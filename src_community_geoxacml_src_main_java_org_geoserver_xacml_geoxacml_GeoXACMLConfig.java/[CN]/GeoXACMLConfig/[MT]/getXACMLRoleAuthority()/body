{
synchronized (raaLock) {
    if (raa != null)     return raa;
    raa=GeoServerExtensions.bean(XACMLRoleAuthority.class);
    if (raa == null) {
      raa=new XACMLDefaultRoleAuthority();
    }
    return raa;
  }
}
