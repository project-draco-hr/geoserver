{
  if (event.getSource() instanceof StyleInfo) {
    try {
      initializeStyles();
    }
 catch (    RuntimeException e) {
      LOGGER.warning(e.getMessage());
    }
catch (    Exception e) {
      LOGGER.warning(e.getMessage());
    }
  }
}
