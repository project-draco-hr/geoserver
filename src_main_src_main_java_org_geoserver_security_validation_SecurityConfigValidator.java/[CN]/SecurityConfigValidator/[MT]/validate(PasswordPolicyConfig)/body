{
  if (config.getMinLength() < 0)   throw createSecurityException(SEC_ERR_40);
  if (config.getMaxLength() != -1) {
    if (config.getMinLength() > config.getMaxLength())     throw createSecurityException(SEC_ERR_41);
  }
}
