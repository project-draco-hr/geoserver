{
  if (oldResourceStore != null && !path.equals(Paths.BASE) && ArrayUtils.contains(dir.getConfig().getIgnoreDirs(),Paths.names(path).get(0))) {
    return oldResourceStore.get(path);
  }
  return new JDBCResource(dir.createEntry(path));
}
