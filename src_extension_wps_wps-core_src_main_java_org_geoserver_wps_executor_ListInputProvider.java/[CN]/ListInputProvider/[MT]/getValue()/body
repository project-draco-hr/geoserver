{
  if (value == null) {
    value=new ArrayList<Object>();
    for (    InputProvider provider : providers) {
      Object pv=provider.getValue();
      value.add(pv);
    }
    providers=null;
  }
  return value;
}
