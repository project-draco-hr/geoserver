{
  UUID eventId=message.getMessageObject();
  AtomicInteger countDown=expectedAckCounters.get(eventId);
  if (countDown != null) {
    countDown.decrementAndGet();
    LOGGER.finer(format("%s - Got ack on event %s from %s",nodeId(),eventId,message.getSource()));
  }
}
