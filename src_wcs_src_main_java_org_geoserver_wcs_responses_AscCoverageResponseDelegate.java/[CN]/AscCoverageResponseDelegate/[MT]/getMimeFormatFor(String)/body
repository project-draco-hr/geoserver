{
  if ("ArcGrid".equalsIgnoreCase(outputFormat))   return "application/arcgrid";
 else   if (isOutputCompressed(outputFormat))   return "application/arcgrid;gzipped=\"true\"";
 else   if (FORMATS.contains(outputFormat))   return outputFormat;
 else   return null;
}
