{
  List<PyObject> args=new ArrayList();
  List<String> kw=new ArrayList();
  int i=0;
  for (  Map.Entry<String,Object> e : parameters.entrySet()) {
    if (PythonDataStoreFactory.TYPE.key.equals(e.getKey()))     continue;
    args.add(Py.java2py(e.getValue()));
    kw.add(e.getKey());
  }
  return type().__call__(args.toArray(new PyObject[args.size()]),kw.toArray(new String[kw.size()]));
}
