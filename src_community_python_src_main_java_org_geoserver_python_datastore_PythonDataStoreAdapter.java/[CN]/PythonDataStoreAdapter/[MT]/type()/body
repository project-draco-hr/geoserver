{
  if (fw.isModified()) {
synchronized (this) {
      if (fw.isModified()) {
        try {
          type=fw.read();
        }
 catch (        IOException e) {
          throw new RuntimeException(e);
        }
      }
    }
  }
  return type;
}
