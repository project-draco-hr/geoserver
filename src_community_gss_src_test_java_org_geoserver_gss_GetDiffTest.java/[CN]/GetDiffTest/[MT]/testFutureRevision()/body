{
  MockHttpServletResponse response=postAsServletResponse(root(true),loadTextResource("GetDiffFuture.xml"));
  validate(response);
  Document dom=dom(response);
  checkOws10Exception(dom,"InvalidParameterValue","fromVersion");
}
