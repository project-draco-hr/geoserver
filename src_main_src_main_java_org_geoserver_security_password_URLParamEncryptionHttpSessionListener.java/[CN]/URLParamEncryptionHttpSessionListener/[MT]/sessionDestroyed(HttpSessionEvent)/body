{
  char[] key=(char[])se.getSession().getAttribute(SESSION_PARAM_KEYNAME);
  if (key != null)   manager.getRandomPassworddProvider().getRandomPassword(key);
  se.getSession().removeAttribute(SESSION_PARAM_KEYNAME);
  se.getSession().removeAttribute(SESSION_PARAM_NAME);
}
