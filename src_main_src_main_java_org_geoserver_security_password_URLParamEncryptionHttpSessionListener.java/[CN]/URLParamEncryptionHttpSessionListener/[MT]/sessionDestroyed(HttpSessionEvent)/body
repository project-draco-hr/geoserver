{
  StandardPBEByteEncryptor enc=(StandardPBEByteEncryptor)se.getSession().getAttribute(SESSION_PARAM_NAME);
  if (enc != null) {
    char[] key=manager.getRandomPassworddProvider().getRandomPasswordWithDefaultLength();
    enc.setPasswordCharArray(key);
  }
}
