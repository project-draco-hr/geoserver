{
  w.writeStartArrayElement("repo");
  w.writeElement("id",repo.getId());
  w.writeElement("name",repo.getRepoName());
  encodeAlternateAtomLink(w,RepositoryProvider.BASE_REPOSITORY_ROUTE + "/" + repo.getRepoName());
  w.writeEndArrayElement();
}
