{
  if (mode == null) {
    mode=Mode.SINGLE;
  }
  if (layers == null) {
    layers=new ArrayList<LayerInfo>();
  }
  if (styles == null) {
    styles=new ArrayList<StyleInfo>();
  }
  if (metadata == null) {
    metadata=new MetadataMap();
  }
}
