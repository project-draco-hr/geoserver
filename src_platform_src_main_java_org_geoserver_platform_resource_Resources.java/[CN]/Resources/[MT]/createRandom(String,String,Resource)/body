{
  long n=random.nextLong();
  if (n == Long.MIN_VALUE) {
    n=0;
  }
 else {
    n=Math.abs(n);
  }
  prefix=(new File(prefix)).getName();
  String name=prefix + Long.toString(n) + suffix;
  return dir.get(name);
}
