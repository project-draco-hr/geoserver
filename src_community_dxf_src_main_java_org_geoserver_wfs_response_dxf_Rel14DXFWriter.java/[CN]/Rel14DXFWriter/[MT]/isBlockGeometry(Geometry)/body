{
  if (geom != null) {
    if (GeometryCollection.class.isAssignableFrom(geom.getClass()))     return true;
    if (Polygon.class.isAssignableFrom(geom.getClass())) {
      return ((Polygon)geom).getNumInteriorRing() > 0;
    }
  }
  return false;
}
