{
  String syntax=object + "." + method+ "(";
  int length=args.length;
  for (int i=0; i < length; ++i) {
    syntax+=args[i];
    if (i != length - 1) {
      syntax+=",";
    }
  }
  return syntax + ")";
}
