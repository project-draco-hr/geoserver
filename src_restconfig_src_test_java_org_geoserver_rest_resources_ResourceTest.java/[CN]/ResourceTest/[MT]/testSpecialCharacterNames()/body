{
  MockHttpServletResponse response=getAsServletResponse("/rest/resource/po??zie/caf??");
  Assert.assertEquals(200,response.getStatusCode());
  Assert.assertEquals("resource",response.getHeader("Resource-Type"));
  Assert.assertEquals("/po??zie",response.getHeader("Resource-Parent"));
}
