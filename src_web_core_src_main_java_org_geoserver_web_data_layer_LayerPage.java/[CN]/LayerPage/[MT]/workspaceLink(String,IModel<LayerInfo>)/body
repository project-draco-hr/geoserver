{
  @SuppressWarnings("unchecked") IModel<String> nameModel=(IModel<String>)WORKSPACE.getModel(model);
  return new SimpleBookmarkableLink(id,WorkspaceEditPage.class,nameModel,"name",(String)nameModel.getObject());
}
