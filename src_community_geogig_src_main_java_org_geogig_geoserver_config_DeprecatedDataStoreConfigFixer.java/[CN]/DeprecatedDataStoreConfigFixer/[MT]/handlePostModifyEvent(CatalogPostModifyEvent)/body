{
  checkNotNull(event);
  try {
    ensureNewConfig(event.getSource());
  }
 catch (  RuntimeException rte) {
    LOGGER.log(Level.WARNING,"Unexpected exception handing post modify event on " + event.getSource() + ". Returning silently to let the catalog go on.",rte);
  }
}
