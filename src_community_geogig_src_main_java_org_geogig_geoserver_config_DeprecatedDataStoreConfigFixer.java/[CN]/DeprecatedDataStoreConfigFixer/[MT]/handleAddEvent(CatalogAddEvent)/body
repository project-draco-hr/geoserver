{
  checkNotNull(event);
  try {
    ensureNewConfig(event.getSource());
  }
 catch (  RuntimeException rte) {
    rte.printStackTrace();
    LOGGER.log(Level.WARNING,"Unexpected exception handing add event on " + event.getSource() + ". Returning silently to let the catalog go on.",rte);
  }
}
