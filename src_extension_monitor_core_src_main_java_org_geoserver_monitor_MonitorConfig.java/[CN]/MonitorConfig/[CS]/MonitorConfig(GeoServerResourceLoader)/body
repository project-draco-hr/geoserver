{
  Resource f=loader.get(Paths.path("monitoring","monitor.properties"));
  if (!Resources.exists(f)) {
    IOUtils.copy(MonitorConfig.class.getResourceAsStream("monitor.properties"),f.out());
  }
  fw=new PropertyFileWatcher(f);
}
