{
  Set<String> expected=ImmutableSet.of(layer1.getResource().getPrefixedName(),layer2.getResource().getPrefixedName(),group1.getName(),group2.getName());
  Set<String> actual=config.getTileLayerNames();
  assertEquals(expected,actual);
}
