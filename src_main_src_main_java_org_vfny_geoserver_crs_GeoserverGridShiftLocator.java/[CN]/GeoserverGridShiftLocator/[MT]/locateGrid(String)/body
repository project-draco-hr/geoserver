{
  if (grid == null)   return null;
  File gridfile=new File(GeoserverDataDirectory.getGeoserverDataDirectory(),"user_projections/" + grid);
  if (gridfile.exists()) {
    return DataUtilities.fileToURL(gridfile);
  }
 else {
    return null;
  }
}
