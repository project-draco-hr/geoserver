{
  InputStream is=WFSNotifyTest.class.getResource("triggers.xml").openStream();
  try {
    OutputStream os=new FileOutputStream(toFile);
    try {
      IOUtils.copy(is,os);
    }
  finally {
      IOUtils.closeQuietly(os);
    }
  }
  finally {
    IOUtils.closeQuietly(is);
  }
}
