{
  encodeStartTag("pos",buff,null);
  buff.append(c.x);
  if (srsDimension == 2)   buff.append(" ").append(c.y);
  if (srsDimension == 3)   buff.append(" ").append(c.z);
  encodeEndTag("pos",buff);
}
