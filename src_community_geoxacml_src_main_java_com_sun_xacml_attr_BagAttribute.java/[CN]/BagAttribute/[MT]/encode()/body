{
  StringBuffer buff=new StringBuffer();
  for (  AttributeValue value : bag)   buff.append(value.encode()).append(",");
  if (buff.length() > 1)   buff.setLength(buff.length() - 1);
  return buff.toString();
}
