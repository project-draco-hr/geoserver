{
  if (dbTestData.isTestDataAvailable()) {
    try {
      getConnection();
      return true;
    }
 catch (    Exception e) {
      LOGGER.log(Level.SEVERE,"Could not obtain connection",e);
    }
  }
  return false;
}
