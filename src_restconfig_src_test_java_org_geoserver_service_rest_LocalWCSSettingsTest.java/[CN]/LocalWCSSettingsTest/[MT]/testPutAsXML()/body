{
  String xml="<wcs>" + "<id>wcs</id>" + "<workspace>"+ "<name>sf</name>"+ "</workspace>"+ "<enabled>false</enabled>"+ "</wcs>";
  MockHttpServletResponse response=putAsServletResponse("/rest/services/wcs/sf/settings",xml,"text/xml");
  assertEquals(200,response.getStatusCode());
  Document dom=getAsDOM("/rest/services/wcs/sf/settings.xml");
  assertXpathEvaluatesTo("false","/wcs/enabled",dom);
}
