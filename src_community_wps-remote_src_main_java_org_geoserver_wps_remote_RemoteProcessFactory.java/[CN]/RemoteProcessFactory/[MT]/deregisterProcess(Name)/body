{
synchronized (names) {
    if (checkName(name)) {
      names.remove(name);
      descriptors.remove(name);
      remoteInstances.remove(name);
      LOGGER.info("Deregistered Service [" + name + "]");
    }
  }
}
