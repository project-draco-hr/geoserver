{
  if (existing != null) {
    if (info != null) {
      OwsUtils.copy(info,existing,clazz);
      merge(existing);
    }
 else {
      delete(existing);
    }
  }
 else {
    if (info != null) {
      persist(info);
    }
  }
}
