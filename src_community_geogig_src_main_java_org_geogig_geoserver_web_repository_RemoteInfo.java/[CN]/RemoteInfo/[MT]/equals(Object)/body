{
  if (!(o instanceof RemoteInfo)) {
    return false;
  }
  if (o == this) {
    return true;
  }
  RemoteInfo r=(RemoteInfo)o;
  return equal(name,r.name) && equal(URL,r.URL) && equal(userName,r.userName)&& equal(password,r.password);
}
